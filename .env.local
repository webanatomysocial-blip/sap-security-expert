# Next.js build-time + runtime environment variables
# INTERNAL_API_URL is set dynamically by start.cjs — keep as-is.
INTERNAL_API_URL=http://127.0.0.1:3000

# Public site URL — baked into the Next.js build at build time.
NEXT_PUBLIC_SITE_URL=https://sap.webanatomy.in
